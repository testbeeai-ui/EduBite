"use client";

import { LoginPage } from "@/components/auth/login-page";

/** Standalone login route — full-screen sign-in, not buried under home content. */
export default function LoginRoute() {
  return <LoginPage />;
}
